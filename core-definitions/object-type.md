# Object Type

* An `Object Type` is used to indicate how to interpret the `Payload`.
* An `Object Type` is represented by a [`string8`](../primitive-types/string8.md).
* An `Object Type` can be [structured](structure.md).