# App

* An `App` is hosted on a `Node`.
* An `App` talks to the `Node` over platform-specific protocols.
* An `App` uses its node to route `Queries` to and from the `Astral Network`.
* An `App` gets an `Identity` assigned by the `Node` it is hosted on.
