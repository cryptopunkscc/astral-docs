# App

* An `App` is hosted on a [`Node`](node.md).
* An `App` talks to the `Node` over platform-specific protocols.
* An `App` uses its node to route [`Queries`](query.md) to and from the `Astral Network`.
* An `App` gets an [`Identity`](identity.md) assigned by the `Node` it is hosted on.
