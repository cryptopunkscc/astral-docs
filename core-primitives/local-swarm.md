# Local Swarm

* A `Local Swarm` means "the `User Swarm` of the [`User`](user.md) that controls me" from
  the perspective of the [`Node`](node.md) (and [`Apps`](app.md) it hosts).
* A `Local Swarm` membership is verified via the cryptographically signed
  [`Objects`](object.md).
