# Swarm

* A `Swarm` is a group of [`Nodes`](node.md) with criteria for membership that can be
  verified by the `Nodes` themselves.
* A `Swarm` of `Nodes` that belong to the same user is called a `User Swarm`.
* A `Swarm` is a generic term and application-specific criteria can be used.
