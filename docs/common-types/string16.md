# string16

Basic string type, equivalent to string8, string32, and string64 in JSON.

## Binary Encoding

A uint16 length prefix followed by the UTF-8 encoded bytes of the string.

## JSON Encoding

A plain JSON string.

## Text Encoding

A plain string.
