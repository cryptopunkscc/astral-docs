# Alias

* An `Alias` is a unique name assigned to an `Identity` be a `Node`.
* An identity can have zero, one or multiple aliases.
* An alias is a string of up to 255 characters. Allowed characters are 
  letters, numbers, an underscore ("__") and a dash ("-").


