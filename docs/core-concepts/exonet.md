# Exonet

* An `Exonet` is any communications protocol that `Nodes` can use to exchange
  binary data with each other.
* An `Exonet` is assumed to be insecure and untrusted.
* The default `Exonet` protocols are TCP, UDP, Tor.
