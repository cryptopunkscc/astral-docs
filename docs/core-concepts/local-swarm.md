# Local Swarm

* A `Local Swarm` means "the `User Swarm` of the `User` that controls me" from
  the perspective of the `Node` (and `Apps` it hosts).
* A `Local Swarm` membership is verified via the cryptographically signed
  `Objects`.
