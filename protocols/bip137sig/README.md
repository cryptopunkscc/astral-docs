# bip137sig

The `bip137sig` protocol exposes BIP-39 seed generation, BIP-32 hierarchical key derivation, and BIP-137 Bitcoin-style message signing. It produces `secp256k1` keys that can be used by the crypto module as a text-signing engine (scheme `bip137`).
