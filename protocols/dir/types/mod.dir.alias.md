# mod.dir.alias

A human-readable name for a node.

The wire form is a `string8`. The text form is the alias string verbatim; JSON encodes the type as that same string.

## Example

```json
{
  "Type": "mod.dir.alias",
  "Object": "alice"
}
```
