# mod.nearby.scan_message

An empty message broadcast to the local network to solicit status responses from nearby nodes.

## Fields

`mod.nearby.scan_message` has no fields. The binary and JSON representations carry no payload.

## Example

```json
{
  "Type": "mod.nearby.scan_message",
  "Object": {}
}
```
