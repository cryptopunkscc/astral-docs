# mod.nodes.new_observed_endpoint_event

Emitted when this node learns of a new externally-observed endpoint for itself (typically reflected back by a peer). Carries no payload.

## Fields

The object has no fields.

## Example

```json
{
  "Type": "mod.nodes.new_observed_endpoint_event",
  "Object": {}
}
```
